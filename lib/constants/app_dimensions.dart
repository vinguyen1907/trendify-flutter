import 'package:flutter/material.dart';

class AppDimensions {
  static const double defaultPadding = 25;
  static const double onboardingPadding = 15;
  static BorderRadius circleCorners = BorderRadius.circular(50);
}
